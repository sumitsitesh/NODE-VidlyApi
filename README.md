# NODE-VidlyApi
#download the code run command (npm install).
#install mongodb and mongodb compass.
#open two command prompt
 1)in 1st cmd your project code will be running 
 2)in 2nd your mongodb will be Running(command to run mongo db- mongod)
#RUN the API's inside postman and check the output in mongo db compass.
